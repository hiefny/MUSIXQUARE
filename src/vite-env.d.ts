/// <reference types="vite/client" />

/**
 * Build-artifact identities injected by the corresponding Vite configs.
 * `typeof` checks keep direct test/module evaluation safely fail-closed.
 */
declare const __MXQR_LEGACY_BOUNDED_BETA_ARTIFACT__: boolean;
declare const __MXQR_LEGACY_BOUNDED_PRODUCTION_ARTIFACT__: boolean;
