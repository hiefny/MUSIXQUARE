/// <reference types="vite/client" />

/**
 * Defined only by the isolated beta-bounded Vite config. `typeof` checks keep
 * ordinary development, production, and test transforms safely undefined.
 */
declare const __MXQR_LEGACY_BOUNDED_BETA_ARTIFACT__: boolean;
